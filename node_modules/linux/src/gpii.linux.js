(function () {

    "use strict";

    var fluid = require("universal");
    var gpii = fluid.registerNamespace("gpii");

    fluid.registerNamespace("gpii.launch");

    gpii.launch.gsettings = function () {
        fluid.log("Success");
    };

})();